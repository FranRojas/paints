let myPaints = [];

function Paints(color, brand, size, quantity){
    this.color=color;
    this.brand=brand;
    this.size=size;
    this.quantity=quantity;


};

function addToList(){
    myPaints.push();

};  
